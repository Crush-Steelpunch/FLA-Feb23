events {}
  http {
    server {
      listen 80;
    location /hello {
    return 200 "<p> My Hostname is {{ ansible_hostname }} </p> <p> My ip is {{ ansible_default_ipv4['address'] }} </p>";
   }
  } 
}
